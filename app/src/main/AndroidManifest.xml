<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sophia.cst2335_final_group_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TeamMainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Food_Nutrition_Tracker"
            android:label="@string/title_activity_food__nutrition__tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AutoList" />
        <activity android:name=".AddAuto" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".EntryList" />
        <activity android:name=".AddEntry" />
        <activity android:name=".DetailActivityT"></activity>
    </application>

</manifest>